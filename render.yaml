services:
  - type: web
    name: flutter-jobs-ch
    env: docker          # Utilisation de Docker au lieu de Node
    region: frankfurt
    plan: free
    
    # Avec Docker, on n'a plus besoin de buildCommand ni startCommand ici
    # car ils sont définis dans le Dockerfile
    
    healthCheckPath: /api/jobs   # Vérifie si l'API répond
    
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: SCRAPE_SECRET
        generateValue: true
